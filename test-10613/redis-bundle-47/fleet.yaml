# Fleet bundle for redis-ns-47
defaultNamespace: redis-ns-47
# (Optional) you can add targetCustomizations if you want to scope to certain clusters,
# e.g.:
# targetCustomizations:
# - name: all-clusters
#   clusterSelector: {}
